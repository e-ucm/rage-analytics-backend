name: "{{sessionId}}"

config:
  topology.workers: 1
  topology.max.spout.pending: 1000
  zookeeperUrl: "{{zookeeperURL}}"
  sessionId: "{{sessionId}}"
  elasticsearchUrl: "{{elasticsearchUrl}}"


topologySource:
  className: "es.eucm.rage.realtime.Analysis"